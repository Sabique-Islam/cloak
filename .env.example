# -----------------------------------------------------------------------------
# `cp .env.example .env` and replace placeholders with real values.
# NEVER commit your real .env into source control.
# -----------------------------------------------------------------------------

# ENV mode
# Use "development" or "production"
ENVIRONMENT=development

# ---------------------------
# Database
# ---------------------------
# Path to SQLite DB file (relative or absolute)
DB_PATH=./cloak.db
# GORM dialect (kept for clarity; currently sqlite)
DB_DIALECT=sqlite

# ---------------------------
# Lab / Domain
# ---------------------------
# Root domain or subdomain you control (no protocol)
LAB_DOMAIN=nopejs.me
# Optional: subdomain prefix used by injectors (e.g., "cloak" -> cloak.nopejs.me)
LAB_SUBDOMAIN=cloak

# ---------------------------
# Github (for repo/commit generation & queries)
# ---------------------------
# Personal access token (repo + workflow if needed). Scope: repo (public), repo:status
GITHUB_TOKEN=ghp_your_personal_access_token_here
# GitHub username or org the lab will use to create repos
GITHUB_OWNER=your_github_username_or_org
# GitHub API base URL (useful for GH Enterprise)
# Leave empty to use https://api.github.com
GITHUB_API_BASE_URL=

# ---------------------------
# Cloudflare (DNS injector)
# ---------------------------
# Cloudflare API token scoped to Zone:DNS:Edit for your LAB_DOMAIN
CLOUDFLARE_API_TOKEN=cf_api_token_here
# Cloudflare Zone ID for the parent zone (e.g., "nopejs.me")
CLOUDFLARE_ZONE_ID=zone_id_here

# ---------------------------
# OSINT Provider API Keys (optional, set only for providers you will poll)
# ---------------------------
# VirusTotal (for URL/domain/file lookups)
VIRUSTOTAL_API_KEY=vt_api_key_here

# GreyNoise (for IP/host context)
GREYNOISE_API_KEY=greynoise_api_key_here

# AlienVault OTX (for indicator lookups)
OTX_API_KEY=otx_api_key_here

# PassiveDNS, Shodan
PASSIVEDNS_API_KEY=
SHODAN_API_KEY=

# ---------------------------
# Worker / Polling / Rate Limits
# ---------------------------
# Number of concurrent worker goroutines for ingestion
WORKER_CONCURRENCY=6
# Max requests per second per provider (used by rate-limiter)
PROVIDER_RPS=5
# Poll interval in seconds for periodic ingestion jobs (0 = disabled)
POLL_INTERVAL_SECONDS=300

# ---------------------------
# App / Server
# ---------------------------
# API server listen address
API_HOST=127.0.0.1
API_PORT=8080

# ---------------------------
# Security / Ops
# ---------------------------
# https://slack.com/intl/en-in/
ALERTS_WEBHOOK_URL=
# https://sentry.io/welcome/
SENTRY_DSN=

# ---------------------------
# Teardown & Safety
# ---------------------------
# If true, generated artifacts will be automatically removed after TEARDOWN_TTL_SECONDS (To be implemented later)
AUTO_TEARDOWN=false
TEARDOWN_TTL_SECONDS=86400

# ---------------------------
# Misc / Experiment Defaults
# ---------------------------
# Default TTL for generated DNS records (in seconds)
DEFAULT_DNS_TTL=120
# Default commit author email used for generated GitHub commits
DEFAULT_COMMIT_EMAIL=
# Default commit author name
DEFAULT_COMMIT_NAME=Cloak_Lab_Bot

# -----------------------------------------------------------------------------
# End of file
# -----------------------------------------------------------------------------
